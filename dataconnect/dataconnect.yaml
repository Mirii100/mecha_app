specVersion: "v1beta"
serviceId: "mecha_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mecha_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
